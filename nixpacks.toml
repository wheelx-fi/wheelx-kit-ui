[phases.setup]
nixPkgs = ["nodejs_20", "pnpm"]

[phases.install]
cmds = ["corepack enable", "pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm demo:build"]

[start]
cmd = "pnpm --dir demo exec next start -p ${PORT:-3000}"
